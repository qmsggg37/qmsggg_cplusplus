#include <iostream>
#include <array>
#include <string>

using namespace std;

// constexpr string DEFAULT_NAME("DEFAULT_NAME");

int main(int argc, char const *argv[])
{
    // array<char, 41> a = {DEFAULT_NAME};

    /* strcpy(&a[0], "hello, world");
       printf("%s\n", &a[0]);
       printf("%s\n", a.data());
     */
    return 0;
}
